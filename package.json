{
  "name": "dachsbau-slots",
  "version": "2.0.0",
  "private": true,
  "description": "Twitch Slot Machine Game - Cloudflare Worker",
  "main": "_worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "wrangler": "^3.0.0"
  }
}
