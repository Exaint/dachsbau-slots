name = "dachsbau-slots"
main = "_worker.js"
compatibility_date = "2024-12-29"

# OPTIMIZED: Smart Placement - Worker runs closer to KV data for lower latency
[placement]
mode = "smart"

# Account ID (optional, aber empfohlen)
account_id = "a532928aecc69ee43a6aafa158d4d788"

# KV Namespace Binding
[[kv_namespaces]]
binding = "SLOTS_KV"
id = "69477a8fb2aa400d9da5496bf6d52ef7"

# D1 Database Binding
[[d1_databases]]
binding = "DB"
database_name = "dachsbau-slots-db"
database_id = "f0129de7-504a-4287-bad2-4b499a341942"

# Observability / Logging
[observability]
[observability.logs]
enabled = true
invocation_logs = true

# Optional: Custom Domain
# routes = [
#   { pattern = "dachsbau-slots.exaint.workers.dev", zone_name = "exaint.workers.dev" }
# ]

# Optional: Compatibility Flags
# compatibility_flags = []

# Optional: Custom Limits (meistens nicht nötig)
# limits = { cpu_ms = 50 }

# Worker renamed to dachsbau-slots (v2)
